export const PORT = 3000;
export const LOCAL_DEV_DOMAIN = `http://localhost:${PORT}`;
export const SIGN_IN_URL = `${LOCAL_DEV_DOMAIN}/sign-in`;
export const ADD_CALENDAR_URL = `${LOCAL_DEV_DOMAIN}/add-calendar`;
